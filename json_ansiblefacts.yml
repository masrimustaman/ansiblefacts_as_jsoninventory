---
  - hosts: all
    tasks:
    - file:
        path: /tmp/out/
        state: directory
      delegate_to: master
    - copy:
        content: "{{ ansible_facts | to_nice_json }}"
        dest: /tmp/out/{{ inventory_hostname }}.json
      delegate_to: master
